# 包目录采用白名单：默认忽略一切，只放行配方与本地辅助文件。
#
# 为什么不是黑名单：下载来的上游文件名不可预测——claude-code 的二进制叫
# claude-2.1.226-x86_64，不带任何扩展名，按后缀写的规则一条都拦不住，
# 结果 565MB 二进制被 git add -A 提交进了历史。白名单没有这个失效模式。
#
# 若某个包新增了需要入库的本地文件（新的扩展名），在下面补一条 ! 例外。

/*/*
# 白名单是按「一级子目录 = 一个包」设计的，非包目录要整个放行，
# 否则 .github/workflows 之类也会被 /*/* 吞掉（踩过一次）
!/.github/**
!/*/PKGBUILD
!/*/*.install
!/*/*.patch
!/*/*.hook
!/*/*.desktop
!/*/*.sh
!/*/*.c
!/*/*.html

# 顶层的工具与配置照常跟踪，仅排除临时产物
nvchecker-new.json
*.log
